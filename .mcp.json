{
  "$schema": "https://raw.githubusercontent.com/modelcontextprotocol/registry/main/schema.json",
  "mcpServers": {
    "c-hr-docs": {
      "command": "node",
      "args": ["mcp/docs-server/index.js"],
      "description": "C-HR docs MCP server. Tools docs_list / docs_search / docs_read scoped to root CLAUDE.md / AGENTS.md / README.md, docs/**, and apps/<name>/CLAUDE.md."
    }
  }
}
