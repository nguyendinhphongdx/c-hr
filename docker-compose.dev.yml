name: c-hr

# Dev mode — only infra (postgres + redis) in containers. BE and FE run
# natively via `pnpm dev` so file watching, debugger, and source maps
# stay snappy. Used by `./scripts/dev.sh start:dev`.
include:
  - ./services/postgres/docker-compose.yml
  - ./services/redis/docker-compose.yml
